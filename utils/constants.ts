export const codeSample = `function greet(name: string): string {
  return \`Hello, \${name}!\`;
}

const message: string = greet("World");
console.log(message);

// This is a sample code snippet
// demonstrating TypeScript syntax`;